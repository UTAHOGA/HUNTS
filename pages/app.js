// Fixing Google Maps API loading timeout and cleanup\n\nlet googleMapsLoadTimeoutId;\n\nfunction loadGoogleMapsAPI() {\n    // Clear the timeout if API loads successfully\n    if (google.maps) {\n        clearTimeout(googleMapsLoadTimeoutId);\n        return;\n    }\n    \n    // Start a new timeout to prevent orphaned timeouts\n    googleMapsLoadTimeoutId = setTimeout(() => {\n        console.error('Google Maps API loading timed out.');\n        cleanup();\n    }, 10000); // Set timeout duration to 10 seconds\n    \n    // ... actual loading logic for the Google Maps API continues here\n}\n\nfunction cleanup() {\n    // Proper cleanup logic here\n    if (googleMapsLoadTimeoutId) {\n        clearTimeout(googleMapsLoadTimeoutId);\n        googleMapsLoadTimeoutId = null;\n    }\n    // Additional cleanup actions if necessary\n}\n\n// Example usage\nloadGoogleMapsAPI();\n